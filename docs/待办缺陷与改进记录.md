# 待办缺陷与改进记录

现行追踪文档：只保留**未完结项的全文**与**已完结项的一行摘要**。
每条细节（现象 / 根因 / 方案对比 / 实施过程）查全量原文：[archive/待办缺陷与改进记录-全量原文.md](archive/待办缺陷与改进记录-全量原文.md)（D1–D18，一字未改）。
用户确认后才执行；标注「待排期」的条目**不实现**。

## 状态一览（D1–D27）

| 编号 | 主题 | 状态 | 提交/详见 |
| --- | --- | --- | --- |
| D1 | 他人语音长对话消息堆叠 | 已实施 | 下方摘要 |
| D2 | 术语库卡片前端细节 | 已实施 | 下方摘要 |
| D3 | 「字幕」快捷键职责与总开关 | 已实施 | `46a41c7` |
| D4 | 字幕浮窗麦克风标识、水平居中 | 已实施 | `46a41c7` |
| D5 | 他人语音译文漏进输入框 | 已修复 | `043ba1a` |
| D6 | 字幕样式 + 渐进式字幕 | 已实施 | `bebbf9e` |
| D7 | 大字幕窗口内容贴底 | 已实施 | `ded3324` |
| D8 | 移除 DeepL / Google | 已实施 | `ded3324` |
| D9 | 快捷键改不动、输入框 X 图标 | 已实施 | `43bed86`、`03f0e3b` |
| D10 | 输入浮窗默认宽度未按 DPI 换算 | 已实施 | `c1f6d73`、`429eb18` |
| D11 | OSC 载荷是否带原文 | 已实施 | `cc80c47` |
| D12 | 小米 MiMo 接入（含翻译服务选型实测） | 已实施 | `fb8ed2f`、`c862cc8` |
| D13 | 腾讯云 / 阿里云翻译从未真正成功过 | 已修复 | `ab40df2`、`41773de` |
| D14 | 两人同时说话只有一路能被识别 | **不修**（产品边界） | 下方全文 |
| D15 | 中文/粤语也送翻译 | **不做**（已拍板） | 下方全文 |
| D16 | 长音频整段识别被截断 | **已关闭**（直喂实验假象，真实路径验证无丢失） | 见 D16 |
| D17 | `VrchatProcessSwitchingTests` 偶发失败 | 已修复 | `1a4a8dd` |
| D18 | 输入框底部被截断 | 已修复 | 下方摘要 |
| D19~D27 | 后续优化：**D20 已实测（声纹切分有效）**；待实施 D22 文本回退 / D23 音源分离 / D24 流式评估（D25~D27 已实施） | **待实施/待评估**（结论见 [模型能力边界.md](模型能力边界.md)，D22 详案见 [archive/后续优化方向-接话与延迟.md](archive/后续优化方向-接话与延迟.md)） | 见下方 |

## 已完结项摘要（细节见全量原文）

- **D1**：他人语音长对话改为消息条滚动列表——每条一句、可上下回看、新消息自动滚到底；不持久化、无清空入口。
- **D2**：术语库卡片删掉保存按钮与重复「启用」文字，标题行与其它卡片对齐；补齐删除后的自动保存触发点。
- **D3**：F7 成为「他人语音 → 字幕」总开关，语音页「打开字幕」按钮删除；任何启停路径结束后窗口可见性 = 识别运行状态。
- **D4**：字幕浮窗去掉易误会的「麦克风」标识，字幕水平居中。
- **D5**：根因是 `SubtitleSpeechSession` 把他人语音译文写进共享预览状态；已改为独立通道。
- **D6**：字幕浮窗样式改造 + 渐进式字幕（分段渐进上屏）落地。
- **D7**：大字幕窗口消息列表改为顶部对齐，高度跟随内容时窗口顶边固定、向下生长。
- **D8**：DeepL / Google 两个提供商删除；老配置回落到仍可用的档案。
- **D9**：三个快捷键改为「点击即录制」的按键捕获框（X 图标随之消失）；后续修正：清空后的快捷键不再在其它页面回落默认值。
- **D10**：输入浮窗默认宽度按 DPI 换算，不再出现高 DPI 下过窄。
- **D11**：OSC 载荷带原文（与 VRChat Chatbox 的显示契约对齐）。
- **D12**：小米 MiMo 接入（`mimo-v2.5` 默认、关思考）；ASR 明确不接（云 ASR 非流式、只支持中英、丢说话人区分，纯亏）。
- **D13**：腾讯/阿里翻译链路修复三个必填/签名问题，此后真实密钥端到端可用。
- **D18**：输入框底部截断——默认高度 `150 → 200` DIP（漏算标题栏与状态行）；已保存过矩形的用户拖高即可，不用清数据。

## 未完结项（D20 已实测；D22~D27 待实施）

结论见 [模型能力边界.md](模型能力边界.md)；D22 详细方案（收益测算/改动点/风险/分阶段验证）见 [archive/后续优化方向-接话与延迟.md](archive/后续优化方向-接话与延迟.md)；声纹专项见 [分析-声纹功能是否保留.md](分析-声纹功能是否保留.md)。

| 编号 | 主题 | 状态 | 成本 | 前置 |
| --- | --- | --- | ---: | --- |
| ~~D19~~ | ~~识别与翻译串行 → 重叠~~ | **已作废**（识别本就不被翻译阻塞；翻译必须串行保证不错序，见方案 §4.1） | — | — |
| **D20** | **声纹切分复测** | ✅ **已完成（结论反转）**，见下 | — | 用真实双人播客实测 |
| **D25** | **删掉头尾预判**（`IsSpeakerChangeSuspected`） | ✅ **已实施** | — | 实测见下 |
| **D26** | **边界变更点改「夹紧」而非丢弃**（`SpeakerSegmentSplitter.Split`） | ✅ **已实施**（新增 8 条单测） | — | 无 |
| **D27** | `MinimumSplitSeconds` 4.5 → 3.0 s（与 D25 同批） | ✅ **已实施** | — | 依据：2.5 s 片 6/24、3.0 s 片 8/20 能给出换人点 |
| **D22** | 文本回退早切（只帮句间停顿 250~600 ms 的人，**不解连说型**） | 待定 | 1.5~2 天 | 可选 |
| **D23** | 音源分离可行性实验（重叠场景唯一方向） | 待实施 | 半天 | 优先级最低 |
| **D24** | **是否引入流式识别**（唯一能解掉「批式固有」两条的路） | **待评估** | 先查资料半天 | 架构级决定 |

### D20 结论（2026-09-16，**实测反转**）

用**真实双人对话播客**（B 站 `BV1rq4CeDE1h`《English Pod》，Marco / Erica 交替说话）实测：

| 项 | 结果 |
| --- | --- |
| **换人切分能不能用** | ✅ **能**。15.21 s 的段切成 3 片，识别文本各自完整，声纹标签 `spk-0001 / spk-0002 / spk-0002`；另一段 12.94 s → 3 片，标签 `0001 / 0002 / 0001`（一来一回） |
| **上次为什么判断「恒为 0 段」** | 当时只有**自拼接的合成音频**——声纹不自然，**不代表真人对话**。该结论作废 |
| **头尾预判** | ❌ **有害**：36 个长段里 **11 段（38%）的真换人被预判挡掉**；且预判为 False 的段 pyannote 只要 33~233 ms——**它没省到钱** |
| 每句真实成本 | 预判 33~50 ms + pyannote **300~900 ms（13 s 段）** + 整句嵌入 50~100 ms |

⇒ 因此新增 **D25 / D26 / D27** 三项小改动（**已实施**），**不删功能、不换模型**。

### D25~D27 实施后的实测（同一素材：播客前 8 分钟，58 个 VAD 段）

| 指标 | 修复前 | **修复后** |
| --- | ---: | ---: |
| **成功切分（≥2 片）** | 约 13 段 | **29 段（翻一倍多）** |
| 切成 3 片 | 13 | 20 |
| 切成 2 片 | 0 | 9 |
| 未切 | 45 | 29 |
| 切分耗时（该素材） | 预判 33~50 ms + 切分 300~900 ms/长段 | 中位 **34 ms**、最大 1.26 s（无预判开销） |

落地改动：

| 编号 | 改动 |
| --- | --- |
| D25 | `ISpeakerIdentifier` 删除 `IsSpeakerChangeSuspected`；`LocalSpeakerIdentifier` 删除该实现与仅供它用的 `FindVoicedWindow`/`Rms`；`PlanSpeakerSpans` 直接调分割 |
| D26 | `SpeakerSegmentSplitter.Split` 把贴边变更点**夹紧**到 `[minPart, total-minPart]`，并加「两片放不下时取中点」的兜底；新增 8 条单测 |
| D27 | `MinimumSplitSeconds` 4.5 → 3.0；`SpeakerIdentifierOptions` 删掉 `ChangeWindowSeconds` / `ChangeSimilarityThreshold`（只服务于已删的预判）；对应单测改写为「分割模型下限」约束 |
| 验证 | 全量 389 条测试连跑 3 次全绿；`Invoke-V2Validation` 通过 |

**分类原则（2026-09-16 用户澄清后确定）**——`[批式固有]` / `[模型固有]` 的完整档案已合并到 **[模型能力边界.md](模型能力边界.md)**：

| 类别 | 内容 | 处理 |
| --- | --- | --- |
| **`[批式固有]`**（不是 bug） | ① 字幕出现的间隔长、文本大块（连说型每 12 秒才一条）；② 说完话到字幕 0.83~1.75 s 里的 600 ms 静音等待 | **只标注，不当 bug 修**——根源是「批式识别必须先切段才能出文本」，换实现去不掉，只有换识别范式（D24）才能解 |
| **`[模型固有]`**（不能修） | 重叠插话丢一路（D14） | 产品边界 |
| **已实测（不是 bug）** | 声纹换人切分：**真人对话上有效**（D20 已完成）；但它带三个可改进点 → D25/D26/D27 | 待实施 |

**用户澄清原话（很重要）**：「**硬切影响的很小，只是字幕出现的间隔长，文本大块大块的**」——
即切在句子中间**不造成明显文字损坏**，真问题是字幕来得疏、一来一大块。

**背景（一句话版）**：

- **快速接话（不重叠）**：本文原有的「合成素材上切分返回 0 段」已被 D20 推翻——**真人对话上切分有效**；真正的问题是**头尾预判挡掉 38% 的真换人**，以及 `MinimumSplitSeconds = 4.5 s` 偏高 → D25/D26/D27 修。详见 [分析-声纹功能是否保留.md](分析-声纹功能是否保留.md)。
- **重叠插话**：单说话人模型的硬上限（与 D14 同源），现状「保一路」是正确取舍——**宁可漏一个人，也不要出乱码**；唯一方向是音源分离（D23）。
- **600 ms 静音等待**：**不能靠调阈值解决**——不同人的句内/句间停顿区间互相重叠（想词型的人句内停顿比连说型的人句间停顿还长）。正确方向是 D22 的「文本回退」（早切 + 用标点判断句子完不完整）。
- **「预判要不要省」已实测回答**：预判 33~50 ms，而 pyannote 在 13~18 s 段上要 0.9~1.0 s → **预判该留**（省的是长段上那 0.9 秒），场景①的病根不是性能。

## 已拍板不做（保留全文）

## D14 · 两个人同时说话：识别层只会跟住一路，另一路整段丢失（**已知硬上限，不修**）

- **现象**：两段不同语种的真人语音按 50% 增益叠加（模拟两人同时讲话）送进内置 SenseVoice，结果只保留其中一路，另一路**整段消失**：

  | 合成音频 | 识别结果 | 丢失 |
  | --- | --- | --- |
  | 中文 5.6 s + 日语 5.6 s 完全重叠 | 「うちの中学は別当生で持ってきない場合は50円の学校販売のパ。」 | 中文整段 |
  | 英语 4.6 s + 韩语 4.6 s 完全重叠 | 「The tribal chieftain called for the boy and presented.」 | 韩语整段 |
  | 中文 5.6 s + 日语后半段插入（半重叠） | 「开饭时间早上9点至下午5点。」 | 日语整段 |

- **影响**：VRChat 里两人抢话是常态。被压住的那一路**不会产生任何字幕**——用户看到的不是「翻译错了」，而是「什么都没出现」。
- **根因**：SenseVoiceSmall 是**单说话人**模型，输入是混合音轨，模型只能跟一路音素；换翻译引擎、调延迟、加中文跳过都无效。
- **可走的方向**（未选型，未排期）：
  1. 用已打包的 pyannote 分割 + CAM++ 声纹先做**说话人分离**，把混合音频切成单说话人片段再分别识别；
  2. 等 sherpa-onnx 的多说话人流式模型（如 streaming zipformer + 说话人分离）。
- **现状**：**不修**。完整档案（现象 / 根因 / 五个无效方向 / 三条可选路线 / 复现方法）见
  [模型能力边界.md](模型能力边界.md) §1；实验数据见 [实验报告-识别与翻译.md](实验报告-识别与翻译.md) §4.4。
- **证据**：`artifacts/mix_zh_ja_over.wav`、`artifacts/mix_en_ko_over.wav`、`artifacts/mix_zh_ja_half.wav`；
  原始记录 `artifacts/asr-matrix.json`（每段 × 5 语言模式 × 10 次）。复现：`pwsh -File artifacts\bench-asr.ps1`。

## D15 · 中文/粤语也送去翻译：白等 100~600 ms，大模型还会改写原意（**已决定：不做**）

- **现象**：他人语音识别出的文本若是纯中文（含粤语书面语），当前代码照常调用翻译服务：
  - 机翻（腾讯 / 阿里通用 / 阿里专业）：**原样返回**，白等 99~108 ms；
  - 大模型（DeepSeek 413 ms / 小米 591 ms）：会**改写原文**，例如「开饭时间早上9点至下午5点。」
    → 小米「用餐时间为上午9点至下午5点。」（chrF2 64.6，用户看到的是被改过的句子）。
- **曾考虑的方案**：`含汉字 且 不含 假名(ぁ-ヿ) / 谚文(가-힣) / 拉丁字母(A-Za-z)` → 跳过翻译，直接复用原文。
  该判定在 27 条评测集 + 4 段音频上**全部判定正确**（见 [实验报告-识别与翻译.md](实验报告-识别与翻译.md) §6.3）；
  若写成「含汉字就跳过」则会把日语/韩语/混说句误判，实测质量从 chrF2 78.6/86.7 **掉到 35.4**。
- **用户决定（2026-09-16）：不实现，保持「一律送翻译」**。理由：
  1. 实测延迟与语种关系不大——纯中文 99~108 ms（机翻）本来就在同一量级，省下的时间不值得为它加一条语言相关分支；
  2. 软件以后可能要扩展给其他语种的人使用，语言相关的特判越多越难改。
- **接受的代价（明确记录，避免以后当成新 bug 再查一遍）**：
  - 机翻档：中文条目**原样返回**，用户看到的字幕与对方原话一致，只是白等 99~108 ms；
  - 大模型档（DeepSeek 93.7 / 小米 64.6 chrF2）：中文条目会被**轻微改写**——例如「这个图好像有人掉线了」
    → DeepSeek「这张图里好像有人掉线了。」。**意思不变、措辞会变**。这个代价已写进引导页推荐文案，
    在意「字幕与原话逐字一致」的用户可按文案选机翻档。
- **状态**：**不做**（decision closed）。若将来真要加，§6.3 的判定规则可直接复用。

## D16 · 长音频（>12 s）被截断，三语连说只认出最后一段（**已关闭：直喂实验的假象**）

- **现象**：18.4 s 的「中文 + 英语 + 韩语」连说音频，识别结果只剩末段韩语 + 一句英文残片
  （`presented 조금만 생각을 하면서 살면 훨씬 편할 거야.`）。
- **结论（2026-09-16，B1+B2 验证完毕，方案《D16与D17修复方案》）**：原实验绕过 VAD
  把整段音频直喂识别器，暴露的是**识别器单段上限**；软件真实路径先过 VAD 切段再逐段识别，
  **内容不丢**。B2 复测（`bench-asr.ps1 -Vad`，生产同款参数与 512 块长）：

  | 素材 | VAD 真实路径 | 直喂对照 |
  | --- | --- | --- |
  | mix_multi3（18.35 s 三语连说） | 3 段：中文「派饭时间早上9点至下午5点。」+ 英语完整一句 + 韩语整句；逐段识别 87~150 ms | 只剩韩语尾巴 |
  | mix_zh_ja_seq（13.39 s 中日两段） | 中文整句 + 日语整句 | 中文被压成「开饭」 |
  | mix_nosil3 / mix_enx3（无静音拼接） | 每段完整恢复，三句英语逐段全对 | 三句糊成一段乱文 |

  复核方独立复跑（2026-09-16）的逐段实测明细，供对照：

  | 素材 | 段号 | 段长 | 段间缺口 | 识别耗时 | 识别文本 |
  | --- | ---: | ---: | ---: | ---: | --- |
  | mix_multi3 | 1 | 4.38 s | 12032 | 129 ms | 派饭时间早上9点至下午5点。 |
  | mix_multi3 | 2 | 5.46 s | 32896 | 150 ms | The tribal chieftain called for the boy and presented him with 50 pieces of gold. |
  | mix_multi3 | 3 | 2.84 s | 30336 | 87 ms | 조금만 생각을 하면서 살면 훨씬 편할 거야.（有少量错字） |
  | mix_zh_ja_seq | 1 | 4.38 s | 12032 | 125 ms | 派饭时间早上9点至下午5点。 |
  | mix_zh_ja_seq | 2 | 5.62 s | 25216 | 161 ms | うちの中学は弁当制で持っていけない場合は五十円の学校販売のパンを買う。 |

  **B2 验收线「三语都出现（允许拆条）」通过**；且每段文本比直喂更完整、更快。
- **强切损耗（实测在案，不修）**：VAD 12 s 强切每次吃约 1280~1344 样本（80~84 ms）、
  检测器预热约 1280 样本、末尾缺口 0~1.93 s 波动（只记录不断言）。**真实语音上 12 s 强切
  从未触发**——实测两段多语素材的**自然语音段最长只有 5.46 s**（mix_multi3 三段 4.38 / 5.46 /
  2.84 s、mix_zh_ja_seq 两段 4.38 / 5.62 s），语言边界与停顿总会先成段；连续 >12 s 不停顿的
  单人说话在实际对话中罕见，即使触发也有 B1 的回归防线兜底（缺口 ≤ 3 窗）。故不做修复。
- **「不明去向样本占比」实测对照（口径必须带上看）**：

  | 素材 | 产出 / 喂入 | 不明去向 |
  | --- | ---: | ---: |
  | 合成信号（18 s 连续正弦+高斯噪声） | 285120 / 288000 | 1.00% |
  | mix_multi3（真实多语语音） | 202880 / 293632 | **30.91%** |
  | mix_zh_ja_seq（真实中日语音） | 160000 / 214272 | **25.33%** |

  **这三个数放在一起会严重误导人**——真实音频那 30% 的**主体不是漏样本，是 VAD 有意丢掉的静音**。
  证据：① 素材源文件本身含尾部静音（`en.wav` 总长 7.15 s，VAD 切出的段只有 5.46 s）；
  ② `mix_audio.py` 在拼接时插入了静音间隔；③ 检测器在段边界会修整静音。
  **真正可能丢内容的量，只有合成信号标定的那两笔**：强切边界约 80 ms + 检测器预热约 80 ms。
  换句话说：**「不明去向」里绝大部分是静音，不是语音**；把它单列出来是为了避免下次误读成「丢了 30%」。
- **识别错字备注**：中文那句首字在真人语音上被识别成「**派**饭时间…」（原素材是「**开**饭时间…」），
  属真实 ASR 错字，**与本条的截断问题无关**；后续若看到「开→派」不要当成 D16 复现。
- **段间缺口在真实素材上不代表强切损耗**：mix_multi3 的缺口 12032 / 32896 / 30336 样本，
  远大于合成信号标定的 1280~1344——因为真实素材含自然停顿与拼接静音，脚本的「缺口」是
  「按定位算出的距离」，不是「强切吃掉的量」。对比 1536 容差时不要混用这两个口径。
- **B1 回归测试**：`SileroVadSegmenterTests.A_continuous_signal_is_force_cut_at_12s_without_eating_speech`
  ——合成 18 s 连续信号（0.3×440 Hz 正弦 + **0.05 高斯**白噪声；必须高斯——均匀噪声实测只出
  1 段 1.56 s，语音判定会被提前打断），按实测容差断言强切段定位/缺口/段长。实测与探测脚本
  逐样本一致：2 段 [192704, 92416]、首段起点 1280、不明去向 1.00%（喂完整尾部后末尾缺口仍为
  256，属 native Flush 的窗取整，非漏喂）。
- **测量脚本注意**：段定位的「32 样本前缀 + 单调游标」对**自我重复素材**（同一录音自拼接）
  会假匹配到更早的重复位置，出现负缺口——mix_enx3 的缺口数字不可信，识别文本结论不受影响。
- **状态**：关闭。

## D17 · `VrchatProcessSwitchingTests` 偶发失败（**已修复**）

- **现象**：全量测试（并行套件）**偶发** 1 条失败：
  `VrchatProcessSwitchingTests.A_published_target_switches_the_capture_and_its_exit_switches_back`。
  同一天两次全量运行一次全绿一次该条失败；**单独重跑该测试类必过**。
- **真实根因（2026-09-16 定位）**：**不是**进程枚举竞争——该测试全程使用假进程源、假采集与脚本延时，
  不碰真实进程枚举。竞态在**测试断言与生产代码的异步间隙**：`AdaptiveLoopbackAudioCapture.InstallAsync`
  先 `SetState`（旧的等待条件在此即满足），再于异步间隙里停掉并释放旧采集；测试等到 SourceKind 翻转
  就断言 `DisposeCount == 1`，线程池紧张时读到中间态的 0 → 偶发失败。单独跑时释放几乎总在断言前完成。
- **修复（只动测试，生产代码零改动，`1a4a8dd`）**：
  1. 等待条件加强到「换流完全落地」：SourceKind 翻转**且**新采集已启动**且**旧采集全部释放完毕（`InstallSettled`）；
  2. `changes` 断言改为锁内快照（写入侧本就加锁，读侧原来是无锁读）；
  3. `FakeLoopbackCapture` 计数改 `Interlocked`、`IsStarted` 改 `volatile`（跨线程轮询的可见性）；
  4. `ProcessTestWait.UntilAsync` 超时可选化（默认仍 5 秒），本测试用 10 秒。
- **验收**：全量套件连跑 **5 次**全绿（76 + 119 + 185 = 380 条/次；D16 的 B1 测试加入后总数为 **381 条**）。
  单跑该类循环**不能**作为验收——单跑无线程池饥饿，全绿证明不了修复。
- **状态**：已修复。
